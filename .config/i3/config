# nThis file has been auto-generated by i3-config-wizard(1).
#
# It will not be overwritten, so edit it as you like.
#
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#
#Needed for i3-gaps
hide_edge_borders both
for_window [class="^.*"] border pixel 3
gaps inner 10
gaps outer 5
focus_on_window_activation focus

#Mod4 - Win
#Mod1 - Alt
set $mod Mod4
#set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font terminus 10
#font pango:Inconsolata 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run -sb "#53B916" -sf "#111111" -nf '#417516'
bindsym $mod+d exec "rofi -modi combi -show combi -combi-modi run,drun"
bindsym $mod+Shift+s exec rofi -show ssh
bindsym $mod+Shift+d exec rofi -show window
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 100
bindsym $mod+Shift+j move down 100
bindsym $mod+Shift+k move up 100
bindsym $mod+Shift+l move right 100

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+semicolon split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
bindsym $mod+z focus child

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
#
# # Show the first scratchpad window
bindsym $mod+minus scratchpad show
#
# # Show the sup-mail scratchpad window, if any.
# bindsym mod4+s [title="^Sup ::"] scratchpad show

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
#bindsym $mod+backslash workspace 11
bindsym $mod+equal workspace 11

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# move workspaces

bindsym $mod+Shift+greater move workspace to output right
bindsym $mod+Shift+less move workspace to output left

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Вийти з i3?' -b 'Так, дійсно хочу закрити WM' 'i3-msg exit'"

bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+l move workspace to output right

#Print screen
#bindsym --release Print exec "scrot ~/Pictures/screen-%d%m%Y_%H-%M-%S.png -e 'feh --scale-down $f; echo $f | xclip -selection primary; xclip -selection clipboard -t image/png < $f'"
##Alt+PrintSceen(Sys_req)
#bindsym --release mod1+Sys_Req exec "scrot -u ~/Pictures/screen-%d%m%Y_%H-%M-%S.png -e 'echo $f | xclip -selection primary; xclip -selection clipboard -t \\"image/png\\" < $f; feh --scale-down $f;'"
#bindsym --release control+Print exec "scrot -s ~/Pictures/screen-%d%m%Y_%H-%M-%S.png -e 'echo $f | xclip -selection primary; xclip -selection clipboard -t \\"image/png\\" < $f; feh --scale-down $f;'"

#PrintScreen (selection)
bindsym --release Print exec "flameshot gui"
#Alt+PrintSceen(Sys_req)
bindsym --release mod1+Sys_Req exec "flameshot full -c -p ~/Pictures/"
bindsym --release control+Print exec "scrot -s ~/Pictures/screen-%d%m%Y_%H-%M-%S.png -e 'echo $f | xclip -selection primary; xclip -selection clipboard -t \\"image/png\\" < $f; feh --scale-down $f;'"


#bindsym $mod+Print exec scrot -s -e 'feh $f'
#bindsym Print exec scrot -e 'feh $f'
#bindsym $mod+Ctrl+Print exec scrot -u -e 'feh $f'
#
#bindsym $mod+Mod1+Print exec scrot -u -e 'geeqie $f'

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym h resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Right resize shrink width 2 px or 2 ppt
        bindsym Down resize grow height 2 px or 2 ppt
        bindsym Up resize shrink height 2 px or 2 ppt
        bindsym Left resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

#обводка_рамки #рамка #текст_назви_вікна #
client.focused #B58900 #B58900 #252525 #FFC510
client.focused_inactive #999999 #4F4F4F #151718 #777777
client.unfocused #555555 #252525 #AAAAAA #A074C4
client.urgent #CE4045 #CE4045 #FFFFFF #DCCD69

#bar {
#    #output DVI-0
#    status_command /usr/bin/py3status
#    #tray_output none
#    position bottom
#  colors {
#    separator #B58900
#    background #252525
#    statusline #B58900
##рамка #фон #цифра
#    #focused_workspace #00BB00 #00BB00 #00000
#    #active_workspace #53B901 #252525 #53B901
#    #inactive_workspace #000000 #417516 #000000
#    #urgent_workspace #474700 #414141 #FFFFFF
#    focused_workspace #B58900 #B58900 #000000
#    active_workspace #B58900 #252525 #B58900
#    inactive_workspace #4F4F4F #252525 #B58900
#    urgent_workspace #000000 #4F4F4F #B58900
#  }
#}

bar {
##    output HDMI-0
    status_command bumblebee-status \
                 -m cpu sensors disk memory xrandr pasink pasource indicator layout datetime \
		 -p \
		    time.format="%H:%M" \
                    nic.exclude='crc,crc-nic,lo,virbr,docker,vboxnet,veth,br,.*:avahi' \
		    nic.format='{state} {ssid}' \
		-t gruvbox
		#-t greyish-powerline
#    #tray_output none
    position top
  colors {
#    separator #00BB00
#    background #252525
#    statusline #005500
#рамка #фон #цифра
    focused_workspace #B58900 #B58900 #000000
    active_workspace #B58900 #252525 #B58900
    inactive_workspace #4F4F4F #252525 #B58900
    urgent_workspace #000000 #4F4F4F #B58900
  }
}

bindsym button3 exec --no-startup-id jgmenu --at-pointer
bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks

bindsym XF86AudioRaiseVolume exec amixer -q set Master 5%+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 5%- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle
bindsym XF86Mail exec urxvt -e mutt
bindsym XF86Calculator exec gnome-calculator
bindsym XF86Explorer exec urxvt -e vifm ~ /media
#bindsym XF86WWW exec Firefox
#bindsym XF86WWW exec qutebrowser
bindsym XF86HomePage exec qutebrowser
#bindsym XF86Suspend exec pm-suspend
#bindsym XF86Battery exec pm-hibernate


bindsym $mod+Next exec amixer -q set Master 5%- unmute
bindsym $mod+Prior exec amixer -q set Master 5%+ unmute
bindsym $mod+M exec amixer -q set Master toggle
bindsym $mod+P exec arandr

bindsym $mod+Q exec qtcreator

bindsym $mod+I exec qutebrowser

bindsym $mod+Mod1+P exec /usr/local/bin/enpass

bindsym $mod+Shift+U exec /usr/local/bin/ultravnc-viewer

#bindsym $mod+Shift+W exec virtualbox --fullscreen --startvm Win7hw

bindsym $mod+Shift+P exec /usr/bin/putty

#bindsym $mod+Mod1+L exec /usr/bin/i3lockr -b 10 -i ~/Pictures/desktop/GL-Zoom-BKRD_bookshelf_6.jpg
bindsym $mod+Mod1+L exec /usr/bin/i3lock-fancy

focus_follows_mouse no

#workspaces on screen
workspace 1 output HDMI-0
workspace 2 output HDMI-0
workspace 3 output HDMI-0
workspace 4 output HDMI-0
workspace 5 output HDMI-0
workspace 6 output HDMI-0
workspace 7 output HDMI-0
workspace 8 output HDMI-0
workspace 9 output HDMI-0
workspace 10 output HDMI-0
workspace 11 output DVI-0

#assign apps to workspaces
assign [class="ffplay"] 10
#assign [class="Firefox"] 7
assign [class="Opera"] 7
assign [class="Claws-mail"] 8
assign [class="Thunderbird"] 8
assign [class="QtCreator"] 4
assign [title="QtConsole"] 4
#viusal studio code
assign [class="Code"] 4

#assign [title="Remmina"] 10

#assign [title="Win7hw"] 11
assign [class="zathura"] 11

#EVE-NG
#for_window [class="konsole" id="0x3000006"] floating enable
#for_window [class="konsole" id="0x2200006"] floating enable
assign [class="konsole"] 11

#spec properties for apps and windows
#(use xprop in command line to get window class)
#(use xwininfo in command line to get widow id)
#
#
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [instance="Download" class="Firefox"] floating enable
for_window [instance="Download" class="Opera"] floating enable
for_window [class="URxvt"] border pixel 2
#for_window [class="XTerm"] floating enable

for_window [class="TeamViewer"] floating enable
for_window [class="Claws-mail" window_role="compose"] floating enable
for_window [class="Claws-mail" window_role="message_search"] floating enable
for_window [class="Krusader" id="0x1e00127"] floating enable
for_window [class="ffplay"] floating enable
for_window [class="Enpass"] floating enable
for_window [class="Keepassx2"] floating enable
for_window [class="Pinentry"] floating enable
for_window [class="Putty"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="gnome-calculator"] floating enable
for_window [class="VirtualBox" id="0x2a012e5"] floating enable
for_window [class="Wireshark"] floating enable
for_window [class="Gajim"] floating enable
for_window [class="org.remmina.Remmina"] floating enable
for_window [class="Seafile Client"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Blueman-manager"] floating enable
for_window [class="zoom"] floating enable
for_window [class="Virt-manager"] floating enable
for_window [class="Com.cisco.anyconnect.gui"] floating enable

#dia
for_window [window_role="toolbox_window"] floating enable
#skype
# for_window [class="Skype"] floating enable, border none, sticky enable
for_window [class="Skype"] floating enable, border none

#spark
#for_window [class="sun-awt-X11-XFramePeer"] floating enable, border none, sticky enable
for_window [class="com-install4j-runtime-launcher-UnixLauncher"] floating enable, border none, sticky enable

#Tickr RSS
for_window [class="Tickr"] floating enable, border none, focus_on_window_activation none, no_focus

#VirtualBox
for_window [id=0x100000d] floating enable
for_window [class="Conky"] floating enable, border none, sticky enable

for_window [class="mpv"] floating enable, border none


#QtCreator
for_window [title="QtConsole"] border 1pixel, floating enable, workspace 4
#, fullscreen toggle
#for_window [title="QtConsole"] border 1pixel, floating enable, workspace 4
#for_window [class="proba"] border 1pixel, floating enable, workspace 4
#VS Code
for_window [class="Code"] border 1pixel, workspace 4

#autorun

# exec --no-startup-id "setxkbmap -layout us,ua"
# exec --no-startup-id "setxkbmap -option 'grp:alt_shift_toggle'"

#exec --no-startup-id "autocutsel -fork -selection CLIPBOARD"
#exec --no-startup-id "autocutsel -fork -selection PRIMARY"
exec --no-startup-id "xset r rate 150 50 -b"
exec --no-startup-id "blueman-applet"
exec --no-startup-id "blueman-tray"
exec --no-startup-id "xautolock -time 30 -locker '/usr/bin/i3lock-fancy'"
exec --no-startup-id "devmon --no-gui"
# exec --no-startup-id "mail-notification"
#exec --no-startup-id "nagstamon"
exec --no-startup-id "nextcloud"
#exec --no-startup-id "gajim"
#exec --no-startup-id "tickr"
#exec --no-startup-id "seafile"
#exec --no-startup-id "xautolock -time 10 -locker 'i3lockr -b 30 -i ~/Pictures/desktop/GL-Zoom-BKRD_bookshelf_6.jpg'"
#exec --no-startup-id "exec --no-startup-id dockd --daemon"
#exec --no-startup-id "exec xcompmgr"
#exec --no-startup-id "compton"
# exec --no-startup-id "telegram-desktop"
exec --no-startup-id "nm-applet"
# exec --no-startup-id "skypeforlinux"
exec --no-startup-id "cbatticon"
# exec --no-startup-id "enpass"
# exec --no-startup-id "gnome-todo"
exec --no-startup-id "Discord"
exec --no-startup-id "feh --bg-fill -rqz ~/Pictures/desktop/"
